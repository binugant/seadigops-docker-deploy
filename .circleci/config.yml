# https://circleci.com/orbs/registry/orb/circleci/aws-ecr
# orbs:
  # orbs:
  # aws-cli: circleci/aws-cli@0.1.13
# version: 2.1
version: 2.1

orbs:
    aws-cli: circleci/aws-cli@0.1.13

workflows:
    "Hello Workflow":
        jobs:
          - aws-cli/hello-build